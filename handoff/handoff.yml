version: 1.0
owner: "GEMINI"
status: "needs_work"

artifacts:
  - path: "src/validate_canonical.py"
    revision: "current"
    last_editor: "GPT"
    handoff_notes: |
      Event ID validator implemented and unit-tested.
      Please generate additional edge-case tests focused on identifiers.

spec_compliance:
  - "docs/data_model_json.md §identifiers - event ID pattern '^ev_\\d{4}$'"

outputs_expected:
  - "New test cases in tests/test_event_id_validation.py (or a new file)"
  - "Sample JSONs in tests/samples/ that trigger each edge case"

acceptance_criteria:
  - "Detects: non-string IDs, uppercase variants, extra digits, underscores, whitespace, leading/trailing spaces, duplicates"
  - "All tests pass locally and in CI"

next_action: "GEMINI: add edge cases + tests; then set owner='COPILOT' with status='needs_review' for PR polish"

ci:
  budget:
    calls_per_day: 1000
    tokens_per_day: 100000

metadata:
  last_updated_by: "GPT"
  notes: "System is green; ready for broader identifier coverage."
