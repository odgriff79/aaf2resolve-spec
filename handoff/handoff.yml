version: 1.0
owner: "GEMINI"
status: "needs_review"

artifacts:
  - path: "src/validate_canonical.py"
    revision: "1.004"
    last_editor: "GPT"
    handoff_notes: |
      Event ID validator in place (pattern ^ev_\d{4}$).
      Added failing samples and pytests asserting CANON-REQ-020.

spec_compliance:
  - "docs/data_model_json.md §identifiers - event ID pattern validation"

outputs_expected:
  - "More edge-case JSONs: ev_12345, EV_1234, ev_000a, missing id, null id, empty string"
  - "One passing JSON (e.g., ev_0000) to confirm zeros are valid"

acceptance_criteria:
  - "Validator emits CANON-REQ-020 for bad IDs"
  - "All existing tests still pass"

next_action: "GEMINI: Generate edge cases and add tests."

metadata:
  created_at: "2025-08-30T00:00:00Z"
  last_updated_by: "GPT"
  last_updated_at: "2025-08-30T00:00:00Z"
