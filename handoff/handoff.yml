# handoff/handoff.yml
version: 1.0
owner: "CL"            # current baton holder: CL | GPT | CO
status: "ready"        # ready | in_progress | needs_review | blocked | completed

artifacts:
  - path: "src/validate_canonical.py"
    revision: "1.001"
    last_editor: "CL"
    handoff_notes: |
      Complete through schema + reason code mapping. 
      GPT: run validator locally & in CI; generate sample reports; raise issues for any spec gaps.
    spec_compliance:
      - "docs/data_model_json.md §Top-level, §Event, §Source, §Effect"
      - "docs/inspector_rule_pack.md §Parameter & keyframe capture"

outputs_expected:
  - "reports/validation/minimal_example.json"
  - "reports/validation/failing_examples.json"
  - "reports/summary/schema_compliance.md"

acceptance_criteria:
  - "Minimal example passes validator with 0 errors"
  - "Known-bad samples trigger expected reason codes"
  - "CI ‘Python setup and validation’ workflow passes on main"

next_action: "GPT: run validator on test samples and commit JSON reports under /reports/validation/"
ci:
  budget:
    calls_per_day: 1000
    tokens_per_day: 100000
  fail_safes:
    on_budget_exceeded: "handoff owner → GPT; set status=blocked; open issue"
metadata:
  created_at: "2025-08-29T00:00:00Z"
  last_updated_by: "OD"
  notes: "Baton file is the single source of truth for agent orchestration."
